import './Banner.css'; // Importing CSS for styling

function Banner() {
    // JSX to render the banner image
  return (
    <header className="banner">
        <img src="/img/banner.png" alt="Banner" />
    </header>
  );
}

export default Banner;